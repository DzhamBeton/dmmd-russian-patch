# Внутренний список задач

## Ближайшее

- [x] Собрать и протестировать следующую версию русификатора с каталогом
  `translations/ru-manual-reviewed.jsonl`.
- [ ] Продолжать ручную редактуру остальных сцен небольшими пакетами через редактор.

## Отложено

- [ ] Перевести глоссарий в меню **Extras**.
  - Текст хранится в изображениях внутри `cg.npk`, а не в сценариях.
  - 13 статей, 15 информационных панелей; статьи 9 и 13 состоят из двух страниц.
  - Также потребуется перевести изображения названий пунктов (`bt01`–`bt13`, состояния
    `on` и `over`).
  - Исходники извлечены в `work/glossary-source/`.
  - При будущей сборке использовать актуальный `cg.npk` русификатора, чтобы сохранить
    вступительную карточку.
- [ ] При желании вернуться к отдельной клавише пропуска кат-сцен: предыдущая реализация
  конфликтовала с управлением или не срабатывала стабильно.
